<% doc_presenter = document_presenter(document) %>
<% access_condition = Ursus::AccessConditionMetadataPresenter.new(document: doc_presenter).access_condition_terms %>

<% if access_condition.length > 0 %>
  <div class='metadata-block'>
    <h4 class='metadata-block__title'>Access Condition</h4>
    <dl class='metadata-block__group'>
      <% access_condition.each do |field_name, field| -%>
        <dt class="blacklight-<%= field_name.parameterize %> metadata-block__label-key">
          <!-- KEY -->
          <%= field.label %>
          <!-- VALUE -->

            <dd class="blacklight-<%= field_name.parameterize %> metadata-block__label-value metadata-block__label-value--ursus">
              <%= doc_presenter.field_value field %>
            </dd>

        </dt>
      <% end %>
      <%= render 'license' %>
    </dl>

    <hr class='divider divider--white'>

  </div>
<% end %>
