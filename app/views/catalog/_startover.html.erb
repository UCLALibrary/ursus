<!-- SINAI -->
<% if Flipflop.sinai? %>
  <% if query_has_constraints? %>
    <%= link_to t('blacklight.search.start_over'), '/catalog?utf8=%E2%9C%93&q=&search_field=all_fields', class: "btn-base btn-base-sm btn-outline-sinai--primary" %>
  <% end %>

<!-- URSUS -->
<% else %>
  <% if query_has_constraints? %>
    <%= link_to t('blacklight.search.start_over'), '/catalog?utf8=%E2%9C%93&q=&search_field=all_fields', class: "btn-base btn-base-sm btn-base--base btn-base--base--ursus" %>
  <% end %>
<% end %>
