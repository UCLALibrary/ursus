<%# Link to the "Next" page
  - available local variables
    url:           url to the next page
    current_page:  a page object for the currently displayed page
    num_pages:     total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<% if current_page.last? %>
    <li class="pagination__page-item disabled">
      <%= link_to raw(t 'views.pagination.next'), '#', :rel => 'next', :onclick=>'return false;', class: 'pagination__page-link', aria: { label: t('views.pagination.aria.go_to_next_page') } %>
    </li>
<% else %>
    <li class="pagination__page-item">
      <%= link_to raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote, class: 'pagination__page-link', aria: { label: t('views.pagination.aria.go_to_next_page') } %>
    </li>
<% end %>
